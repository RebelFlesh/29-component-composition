import React, { Component } from 'react';

export default class Landing extends Component {
  render(){
    return (
      <React.Fragment>
        More text to test landing
      </React.Fragment>
    );
  }
}